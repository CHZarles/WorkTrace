# v0.1.28（Windows）

- 自动更新增加下载进度显示：
  - 更新安装过程中显示准备状态、下载状态和启动安装器状态。
  - 下载阶段显示已下载大小、总大小和百分比进度条。
- 自动更新移除下载相关超时：
  - GitHub 最新版本检查不再使用固定超时。
  - 安装包下载不再使用固定超时，较慢网络下不会因为超时中断。
- 更新安装流程保持原有行为：
  - 仍会先停止本地 agent，再拉起外部更新器执行安装与重启。

下载与更新：
- 安装版：`WorkTrace-v0.1.28-windows-setup.exe`
