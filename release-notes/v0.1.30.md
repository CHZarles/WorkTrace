# v0.1.30（Windows）

- 修复 GitHub Windows 打包流程：
  - Release workflow 里的 Flutter Windows 工程目录从旧的 `recorderphone_ui` 切换为 `worktrace_ui`。
  - 远程打包步骤与当前仓库里的打包脚本、UI 工程目录保持一致。
- 保持 `WorkTrace` 品牌与 `worktrace_ui` 工程命名一致：
  - 远程构建不再因为旧目录名缺失而在 `Package (installer)` 步骤失败。

下载与更新：
- 安装版：`WorkTrace-v0.1.30-windows-setup.exe`
