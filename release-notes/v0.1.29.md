# v0.1.29（Windows）

- 品牌与工程标识统一为 `WorkTrace`：
  - 清理遗留的 `RecorderPhone` / `recorderphone_ui` 标识。
  - Flutter 独立 UI 目录统一为 `worktrace_ui`。
  - Windows 工程与解决方案统一为 `WorkTrace.Windows`。
  - 协议、脚本、Schema、文档里的对外名称同步为 `WorkTrace`。
- Planner 继续打磨 UI 与交互：
  - 顶部信息层级重排，新增更明确的当前视图摘要和 `Today` 快捷入口。
  - TODO 列表卡片补充日期、时间、提醒等状态标签，搜索区更紧凑。
  - Calendar 周视图提升小时高度和事件块可读性，拖拽改期与空白处创建更直接。
  - 月视图与 Agenda 侧栏的信息反馈更集中，日期聚焦更清晰。
- 文档同步更新：
  - README、发布说明、Windows 开发说明与研究文档已同步当前产品名与用法。

下载与更新：
- 安装版：`WorkTrace-v0.1.29-windows-setup.exe`
